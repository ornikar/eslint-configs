const objectWithProperties = {
  camelCase: true,
  snake_case: true,
};

// eslint-disable-next-line camelcase
const variable_in_snake_case = true;

// eslint-disable-next-line camelcase
export { objectWithProperties, variable_in_snake_case };
